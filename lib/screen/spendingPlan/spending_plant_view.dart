import 'package:flutter/material.dart';

class SpendingPlantView extends StatelessWidget {
  const SpendingPlantView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "spendingPlant Page",
        style: TextStyle(fontSize: 24),
      ),
    );
  }
}
